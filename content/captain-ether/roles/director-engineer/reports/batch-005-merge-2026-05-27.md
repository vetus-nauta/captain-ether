# Batch 005 Merge

Date: 2026-05-27
Role: Director-Engineer / Captain Ether

## Status

MERGED LOCALLY / NEEDS VALIDATOR ENVIRONMENT BEFORE DEPLOY.

Batch 005 `Urgency / Pan-Pan Equipment And Assistance Basics` is merged into
local playable Captain Ether content and regression source.

This does not approve production deploy, FTP upload, public production smoke,
runtime/API/UI changes, matcher changes, router/registry, auth/platform, Watch
Officer, Nav Desk, Game Director docs, or production config changes.

## Merge Gate

Inputs accepted:

- Content Producer:
  `content/captain-ether/roles/content-producer/reports/batch-005-urgency-panpan-equipment-assistance-card-2026-05-27.md`
- Sea Speak Linguist:
  `content/captain-ether/roles/sea-speak-linguist/reports/batch-005-urgency-panpan-risk-review-2026-05-27.md`
- Engineering gate:
  `content/captain-ether/roles/director-engineer/reports/batch-005-engineering-gate-2026-05-27.md`
- QA acceptance:
  `content/captain-ether/roles/qa/reports/batch-005-urgency-panpan-acceptance-qa-2026-05-27.md`
- Director acceptance:
  `content/captain-ether/roles/director-engineer/reports/batch-005-qa-accepted-2026-05-27.md`

## Changed Content

- `content/captain-ether/starter.json`
- `content/captain-ether/accept-reject-qa-pairs.json`
- `content/captain-ether/batches/batch-005-urgency-panpan-equipment-assistance-basics.json`
- `content/captain-ether/roles/director-engineer/reports/batch-005-merge-2026-05-27.md`

## Merge Notes

- `25` Batch 005 items were added to `starter.json`.
- Batch item `qa_notes` were converted into `accept-reject-qa-pairs.json`.
- Batch item `qa_notes` were not copied into playable runtime items.
- `8` Batch 005 dangerous minimal-pair groups were added to regression.
- Batch file status is now `merged`.
- No matcher/API/UI/policy change was made.

## Final Local State

Playable content:

| Metric | Count |
| --- | ---: |
| Items | `255` |
| Grammar patterns | `112` |
| Scenarios | `2` |

Type counts:

| Type | Count |
| --- | ---: |
| `word` | `50` |
| `short_expression` | `69` |
| `phrase` | `136` |

Level counts:

| Level | Count |
| --- | ---: |
| `beginner` | `88` |
| `intermediate` | `118` |
| `advanced` | `49` |

Branch counts:

| Branch | Count |
| --- | ---: |
| unbranched legacy starter | `40` |
| `core_radio` | `50` |
| `marina_harbour` | `50` |
| `navigation_reports` | `50` |
| `safety_securite` | `40` |
| `urgency_panpan` | `25` |

Regression source:

| Metric | Count |
| --- | ---: |
| QA item entries | `255` |
| Should-accept examples | `710` |
| Should-reject examples | `784` |
| Dangerous minimal-pair groups | `57` |
| Dangerous must-accept examples | `140` |
| Dangerous must-reject examples | `151` |

## Checks Run

```sh
jq empty content/captain-ether/starter.json
jq empty content/captain-ether/accept-reject-qa-pairs.json
jq empty content/captain-ether/batches/batch-005-urgency-panpan-equipment-assistance-basics.json
```

Result: PASS.

```sh
jq '{items:(.items|length), grammar_patterns:(.grammar_patterns|length), scenarios:(.scenarios|length), qa_notes_in_starter:([.items[] | select(has("qa_notes"))] | length), duplicate_ids:((.items|length)-(.items|map(.id)|unique|length))}' content/captain-ether/starter.json
```

Result:

- `255` items;
- `112` grammar patterns;
- `2` scenarios;
- `0` playable items with `qa_notes`;
- `0` duplicate item IDs.

```sh
jq '{items:(.items|length), should_accept:([.items[].should_accept[]?]|length), should_reject:([.items[].should_reject[]?]|length), dangerous_minimal_pairs:(.dangerous_minimal_pairs|length), duplicate_item_ids:((.items|length)-(.items|map(.id)|unique|length))}' content/captain-ether/accept-reject-qa-pairs.json
```

Result:

- `255` QA item entries;
- `710` should-accept examples;
- `784` should-reject examples;
- `57` dangerous minimal-pair groups;
- `0` duplicate QA item IDs.

## Validator Status

The PHP validator was not run because `php` is not available on `PATH` in this
shell.

Attempted command:

```sh
command -v php
```

Result: no output.

This is a blocker for any deploy or production smoke decision. Before deploy,
run:

```sh
php content/captain-ether/tools/validate-captain-ether.php
```

in an environment where PHP is available, then route the result through QA or
Director-Engineer acceptance.

## Next Expected

1. Run PHP validation in an environment with PHP.
2. If validation passes, assign QA local post-merge review or Director-Engineer
   acceptance for production-deploy planning.
3. Production deploy remains closed until a separate Game Director deployment
   task.

## Scope Preserved

- Matcher/API/runtime not changed.
- UI not changed.
- Router/registry not changed.
- Auth/platform not changed.
- Watch Officer not changed.
- Nav Desk not changed.
- Game Director docs not changed.
- Production config and deploy/FTP not touched.
- Secrets, cookies, sessions, CSRF, player email, and player identity not
  touched or printed.
