# Technical Card: Batch 004 Merge

Status: PASS
Date: 2026-05-27
Role: Director-Engineer / Captain Ether

## Result

Batch 004 Safety / Securite Warnings is merged into playable Captain Ether
content.

## Merge Gate

QA report:

```text
content/captain-ether/roles/qa/reports/batch-004-acceptance-qa-2026-05-27.md
```

Result: `PASS`.

## Changed Content

- `content/captain-ether/starter.json`
- `content/captain-ether/accept-reject-qa-pairs.json`
- `content/captain-ether/batches/batch-004-safety-securite-warnings.json`
- `content/captain-ether/roles/director-engineer/reports/batch-004-merge-2026-05-27.md`

## Final Local State

- Playable items: `230`
- Grammar patterns: `112`
- Scenarios: `2`
- Regression QA items: `230`
- Should-accept examples: `631`
- Should-reject examples: `709`
- Dangerous minimal-pair groups: `49`

Type counts:

- `46` word
- `62` short_expression
- `122` phrase

Level counts:

- `84` beginner
- `109` intermediate
- `37` advanced

Branch counts:

- `40` legacy starter items without branch metadata
- `50` core_radio
- `50` marina_harbour
- `50` navigation_reports
- `40` safety_securite

## Merge Notes

- `40` Batch 004 items were added to `starter.json`.
- `24` Batch 004 grammar patterns were added to `starter.json`.
- Batch item `qa_notes` were converted into `accept-reject-qa-pairs.json`.
- Batch item `qa_notes` were not copied into playable runtime items.
- `batch-004-safety-securite-warnings.json` is now marked `merged`.
- No matcher/API/policy/UI change was needed for this merge.

## Validation

Local full validation passed:

- all `230` `target_text` values pass;
- all `631` should-accept examples pass;
- all `709` should-reject examples stay wrong;
- all `49` dangerous minimal-pair groups pass;
- watch-selection smoke passed for beginner, intermediate, and advanced;
- all smoke runs preserved progressive order:

```text
word -> short_expression -> phrase
```

Known warnings:

- `6` existing starter duplicate-normalization warnings remain from old starter
  items.
- They are non-blocking and unrelated to Batch 004.

## Production Deploy

Production deploy completed:

- merged Captain Ether content and role docs were uploaded and hash-checked;
- `12` files passed remote hash verification;
- `https://game.brkovic.ltd/games/captain-ether` returned HTTP `200`;
- route HTML contained the Captain Ether page text.

Production QA smoke is assigned separately:

```text
content/captain-ether/roles/qa/tasks/batch-004-production-smoke-2026-05-27.md
```

Expected report:

```text
content/captain-ether/roles/qa/reports/batch-004-production-smoke-2026-05-27.md
```

## Not Touched

- matcher/API files;
- router;
- registry;
- Nav Desk;
- Watch Officer;
- auth/platform files;
- Captain Ether UI;
- private config, `.netrc`, SMTP, cookies, login codes, or secrets.
