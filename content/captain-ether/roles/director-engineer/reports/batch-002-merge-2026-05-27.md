# Technical Card: Batch 002 Merge

Status: PASS  
Date: 2026-05-27  
Role: Director-Engineer / Captain Ether

## Result

Batch 002 is merged into playable Captain Ether content and deployed to
production.

## Merge Gate

QA report:

```text
content/captain-ether/roles/qa/reports/batch-002-acceptance-qa-2026-05-27.md
```

Result: `PASS`.

## Changed Content

- `content/captain-ether/starter.json`
- `content/captain-ether/accept-reject-qa-pairs.json`
- `content/captain-ether/batches/batch-002-marina-harbour-basics.json`
- `content/captain-ether/roles/director-engineer/reports/batch-002-merge-2026-05-27.md`

## Final Local State

- Playable items: `140`
- Grammar patterns: `61`
- Scenarios: `2`
- Regression QA items: `140`
- Should-accept examples: `401`
- Should-reject examples: `435`
- Dangerous minimal-pair groups: `27`

## Merge Notes

- `50` Batch 002 items were added to `starter.json`.
- `22` Batch 002 grammar patterns were added to `starter.json`.
- Batch item `qa_notes` were converted into `accept-reject-qa-pairs.json`.
- Batch item `qa_notes` were not copied into playable runtime items.
- `batch-002-marina-harbour-basics.json` is now marked `merged`.

## Verification

Local matcher regression passed:

- all `140` `target_text` values pass;
- all `401` should-accept examples pass;
- all `435` should-reject examples stay wrong;
- all `27` dangerous minimal-pair groups pass;
- specific `berth/birth` and `fender/finder(s)` checks pass.

Watch-selection smoke passed for:

- beginner: `12` questions;
- intermediate: `16` questions;
- advanced: `20` questions.

All smoke runs preserved progressive order:

```text
word -> short_expression -> phrase
```

Production deploy:

- uploaded merged Captain Ether content, matcher, policy, and role docs;
- hash-check passed for `14` files;
- `https://game.brkovic.ltd/games/captain-ether` returned HTTP `200`.

Production QA smoke:

```text
content/captain-ether/roles/qa/reports/batch-002-production-smoke-2026-05-27.md
```

Result: `PASS`.

QA confirmed:

- production route/login works;
- watches are `12` / `16` / `20`;
- progressive order is preserved;
- `21` Batch 002 item IDs were observed live;
- player-facing payload did not expose `target_text`, `accepted_answers`, or
  `qa_notes`;
- targeted `berth/birth` and `fender/finder(s)` checks passed.

## Not Touched

- router;
- registry;
- Nav Desk;
- Watch Officer;
- auth/platform files;
- Captain Ether UI;
- private config, `.netrc`, SMTP, cookies, login codes, or secrets.

## Next Step

MVP readiness analysis:

```text
content/captain-ether/roles/director-engineer/reports/mvp-readiness-analysis-2026-05-27.md
```
